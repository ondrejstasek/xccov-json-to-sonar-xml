# xccov-json-to-sonar-xml

Convert Xcode code coverage report (.xcresult) to SonarQube generic xml code coverage report.

Usage:

```bash
swift run xccov-json-to-sonar-xml --xcresult-path <path> --output-file <path>
```
